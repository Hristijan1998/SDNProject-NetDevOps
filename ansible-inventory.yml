all:
  vars:
    # The generated inventory is assumed to be used from the clab host.
    # Hence no http proxy should be used. Therefore we make sure the http
    # module does not attempt using any global http proxy.
    ansible_httpapi_use_proxy: false
  children:
    linux:
      hosts:
        clab-sdn-project-controller1:
          ansible_host: 
        clab-sdn-project-controller2:
          ansible_host: 
        clab-sdn-project-pc1:
          ansible_host: 
        clab-sdn-project-pc2:
          ansible_host: 
        clab-sdn-project-s1:
          ansible_host: 
        clab-sdn-project-s2:
          ansible_host: 
        clab-sdn-project-s3:
          ansible_host: 
        clab-sdn-project-s4:
          ansible_host: 